[project]
name = "soilwise-dissemination-guidelines"
version = "0.1.0"
description = "MkDocs site for the SoilWise Dissemination & Guidelines and User Manual"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
    "mkdocs-awesome-pages-plugin>=2.10",
    "pymupdf>=1.26",
]

[tool.uv]
package = false